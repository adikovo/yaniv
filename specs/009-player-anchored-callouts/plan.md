# Implementation Plan: Player-Anchored Yaniv/Asaf Call-Outs

**Branch**: `009-player-anchored-callouts` | **Date**: 2026-06-10 | **Spec**: [spec.md](spec.md)

## Summary

Replace the centered round-end overlay with comic-style "YANIV!" / "ASAF!" call-outs anchored to the acting player's area on every client. "YANIV!" appears over the caller; in an Asaf round, "ASAF!" follows over the countering player a beat later, conveying the +30 penalty. The server adds an explicit `yanivCaller` field to `roundEnd` (replacing the confusing `asafCaller`), so the client anchors call-outs by simple id comparison. Rounds already auto-advance (now after 4s).

---

## Technical Context

**Language/Version**: JavaScript (React 19, Node.js/Express)

**Primary Dependencies**: React, Socket.io (client + server), Vite, React Router

**Storage**: N/A (no persistence; all state is in-memory on server + React state on client)

**Testing**: TDD — Vitest + React Testing Library in `client/` (pattern: `*.test.jsx` next to the component, see `opponent-area.test.jsx`); component tests are written **before** the implementation for every behavior change. A Playwright e2e smoke test (`e2e/four-player-game.spec.ts`) runs at the very end as final validation of the full flow.

**Target Platform**: Mobile web (portrait), primary; desktop secondary

**Project Type**: Real-time multiplayer web game (React client + Express/Socket.io server)

**Performance Goals**: Call-out appears on all clients as soon as `roundEnd` is received (within 1s of the Yaniv call)

**Constraints**: Call-out sequence must fit inside the existing round-transition window (server deals next round 4s after `roundEnd`; clients dismiss the result 1.5s after `nextRound` — ~5.5s total); 2–4 players; CSS-only visuals (no image assets)

**Scale/Scope**: 2–4 concurrent players per room, plus spectators

---

## Constitution Check

Constitution template is unfilled — no gates to enforce. Proceeding.

---

## Key Existing Behavior (verified in code)

- `server/socket.js:112` emits `roundEnd` with `{ winner: {id, name}, asaf, asafCaller: {id, name} | null, players, eliminated }`.
  - **Semantics**: `winner` is the round winner. When `asaf` is true, `winner` is the *countering* player and `asafCaller` is the *original Yaniv caller* (who received the +30 penalty). When `asaf` is false, `winner` *is* the Yaniv caller.
  - **This feature makes the caller explicit**: `gameLogic.yanivCall` returns the caller, and `roundEnd` carries `yanivCaller: {id, name}` instead of `asafCaller` (whose only consumer is the overlay being deleted). Client anchoring is then plain id comparison: "YANIV!" on `yanivCaller.id`; "ASAF!" on `winner.id` when `asaf`.
- `server/socket.js:142` auto-deals the next round 4000ms after `roundEnd` (no user interaction).
- `client/src/pages/game/index.jsx` stores the `roundEnd` payload in `yanivResult`, renders `<YanivOverlay>` (centered, fixed) in both the main and spectator views, and clears `yanivResult` 1500ms after `nextRound` — which also drives the eliminated→spectator prompt. That dismissal logic must be preserved.
- Opponents render via `OpponentArea` (`client/src/components/opponent-area/index.jsx`) with a `position` prop (`top`/`left`/`right`); the local player renders in `.local-player-area`. The spectator view renders *all* players as `OpponentArea`s. Anchoring call-outs inside these containers covers every seat, player count, and the spectator case with no coordinate math.

---

## Project Structure

### Documentation (this feature)

```text
specs/009-player-anchored-callouts/
├── plan.md              ← this file
├── spec.md
├── research.md
├── data-model.md
└── tasks.md             (generated by /speckit-tasks)
```

### Source Code Changes

```text
server/
├── gameLogic.js                 ← yanivCall returns the caller
├── gameLogic.test.js            ← NEW/extended Jest tests (TDD)
└── socket.js                    ← roundEnd: + yanivCaller, − asafCaller

client/src/
├── components/
│   ├── call-out/                ← NEW component
│   │   ├── index.jsx
│   │   ├── call-out.test.jsx
│   │   └── styles.css
│   ├── opponent-area/
│   │   ├── index.jsx            ← add `callout` prop, render <CallOut>
│   │   ├── opponent-area.test.jsx
│   │   └── styles.css           ← position: relative (anchor context)
│   └── yaniv-overlay/           ← DELETED (replaced by call-out)
├── hooks/
│   ├── use-asaf-sequence.js     ← NEW: Asaf timing beat
│   └── use-asaf-sequence.test.js
└── pages/game/
    ├── index.jsx                ← anchor call-outs by id; remove YanivOverlay
    └── styles.css               ← ensure .local-player-area anchors the call-out

e2e/four-player-game.spec.ts     ← update: assert call-out anchoring instead of old overlay
```

---

## Phase 1: Server — Explicit `yanivCaller` in `roundEnd`

### Change: `server/gameLogic.js` (+ Jest test, TDD)

`yanivCall(game)` already binds `const caller = getCurrentPlayer(game)`. Return it: `return { winner, asaf, caller }` (drop the `asafCaller: asaf ? caller : null` indirection). Jest test first: asserts the returned `caller` is the calling player in both the normal and asaf cases, and that `winner` is the counterer when asaf.

### Change: `server/socket.js`

In the `roundEnd` emit (~line 112): replace `asafCaller: asafCaller ? {...} : null` with `yanivCaller: { id: caller.id, name: caller.name }`. `asafCaller` is removed from the payload — its only consumer is the old centered overlay, deleted in Phase 6.

## Phase 2: New `CallOut` Component

### New file: `client/src/components/call-out/index.jsx`

Props: `{ variant, penalty }` — `variant: 'yaniv' | 'asaf'`, `penalty: boolean` (show "+30" subtext on the Asaf call-out).

```jsx
import './styles.css';

export const CallOut = ({ variant, penalty }) => (
    <div className={`call-out call-out-${variant}`}>
        <span className="call-out-text">{variant === 'yaniv' ? 'YANIV!' : 'ASAF!'}</span>
        {penalty && <span className="call-out-penalty">+30</span>}
    </div>
);
```

### New file: `client/src/components/call-out/styles.css`

- `.call-out`: `position: absolute`, centered over the parent area (`top: 50%; left: 50%; translate(-50%, -50%)`), `z-index: 60`, `pointer-events: none`.
- `.call-out-text`: large comic-style lettering — bold italic, ~2.2rem (opponents) / scalable, yellow→orange gradient fill (`background-clip: text`) with dark outline via layered `text-shadow` (no `-webkit-text-stroke` dependency alone), slight rotation (−6deg) for the comic feel.
- `.call-out-asaf .call-out-text`: red/orange variant to distinguish from Yaniv.
- `.call-out-penalty`: smaller dark badge under the text ("+30").
- Pop-in animation: `@keyframes call-out-pop` scaling 0 → 1.15 → 1 over ~350ms.
- Must stay within viewport: `max-width: 90vw`, no fixed widths that overflow on the left/right seats.

---

## Phase 3: `OpponentArea` — Call-Out Anchor

### Change: `client/src/components/opponent-area/index.jsx`

Add a `callout` prop: `null | { variant: 'yaniv' | 'asaf', penalty: boolean }`. Render `<CallOut {...callout} />` inside the container so it centers over the opponent's hand:

```jsx
export const OpponentArea = ({ name, handCount, score, isActive, position, callout }) => (
    <div className={`opponent-area opponent-${position} ${isActive ? 'active-turn' : ''}`}>
        ...
        {callout && <CallOut variant={callout.variant} penalty={callout.penalty} />}
    </div>
);
```

### Change: `client/src/components/opponent-area/styles.css`

Ensure `.opponent-area { position: relative; }` so the absolute call-out anchors to it (verify — may already be set).

---

## Phase 4: Local Player Area — Call-Out Anchor

### Change: `client/src/pages/game/index.jsx` + `styles.css`

Render the same `<CallOut>` inside `.local-player-area` when the local player is the actor. Ensure `.local-player-area { position: relative; }`. The call-out centers over the local hand (per spec acceptance scenario 2 of US1).

---

## Phase 5: Game Page — Anchoring by ID, Sequencing, Removal of Old Overlay

### New hook: `client/src/hooks/use-asaf-sequence.js` (+ test, TDD)

`useAsafSequence(yanivResult)` → `showAsaf: boolean`. `false` initially; when `yanivResult?.asaf` is true, a ~1500ms timer flips it to `true`; resets to `false` when `yanivResult` clears; timer held in a ref and cleaned up on change/unmount. Tested with `renderHook` + fake timers.

### Change: `client/src/pages/game/index.jsx`

1. `const showAsaf = useAsafSequence(yanivResult);`
2. Per-player call-out by plain id comparison against the new explicit payload fields (no derivation helper):
   ```jsx
   const calloutFor = (id) =>
       !yanivResult ? null
       : id === yanivResult.yanivCaller.id ? { variant: 'yaniv', penalty: false }
       : yanivResult.asaf && showAsaf && id === yanivResult.winner.id ? { variant: 'asaf', penalty: true }
       : null;
   ```
3. Pass `callout={calloutFor(p.id)}` to every `OpponentArea` (main **and** spectator views) and render `<CallOut>` inside `.local-player-area` when `calloutFor(player.id)` is non-null.
4. Remove both `<YanivOverlay>` renders and the import.

**Timing fit**: `roundEnd` at t=0 → YANIV! pops; ASAF! at t≈1.5s; server `nextRound` at t=4s; `yanivResult` cleared at t=5.5s → call-outs unmount. The eliminated→spectator-prompt logic inside the existing dismissal callback is untouched, satisfying FR-007/FR-008.

**Disconnect edge case**: if the actor disconnected, their `OpponentArea` no longer renders, so the call-out is simply absent — graceful degradation per spec; the round still advances on the server timer.

---

## Phase 6: Delete `yaniv-overlay` Component

Remove `client/src/components/yaniv-overlay/` (index.jsx + styles.css) once nothing imports it (FR-006).

---

## Phase 7: E2E Smoke Test (final validation)

### Change: `e2e/four-player-game.spec.ts`

Runs at the end, after all TDD phases are green, as the final validation of the full flow. The smoke test currently waits for the old overlay (`.yaniv-overlay`) at round end. Update it to:
- Wait for `.call-out-yaniv` and assert it is rendered *inside* the acting player's area (`.opponent-area` of the caller on other clients, `.local-player-area` on the caller's client).
- Keep the rest of the flow (round completes, next round deals) intact.
- Run the full Playwright suite headed/headless as the last task of the feature.

## TDD Workflow (applies to Phases 1–5)

For each phase: write the failing test first — Jest in `server/` for Phase 1 (`cd server && npm test`), Vitest colocated `*.test.jsx` in `client/` for Phases 2–5 (`cd client && npm run test`) — see it fail, implement, re-run to green. Phase 6 (deletion) is covered by the suite staying green; Phase 7 is the closing e2e smoke test.

---

## Complexity Tracking

No constitution violations. One small server payload change (explicit `yanivCaller`), one new component, one new hook, two prop additions, one deletion.
